{
    "description": "a starter, CLI tool template",
    "variables": {
        "PROGRAM_NAME": {
            "summary": "the name of the program",
            "default": "/${PROJECT_NAME}/\\w+/\\L\\0\\E/\\W+/-/",
            "pattern": "^[[:word:]-]+$"
        },
        "PROJECT_SUMMARY": {
            "summary": "a very short summary of the program",
            "default": "A Vala CLI Tool"
        }
    },
    "templates": [
        "src/main.vala",
        "src/meson.build",
        "meson.build",
        "README.md"
    ]
}
