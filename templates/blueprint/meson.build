project(
    '${PROJECT_NAME}',

    ['c', 'vala'],
    version: '${PROJECT_VERSION}'
)

pkg = import('pkgconfig')
gnome = import('gnome')

project_name = meson.project_name()

subdir('data')
subdir('src')
